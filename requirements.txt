# GNNShield - DDoS Detection System Requirements
# ================================================
# Install with: pip install -r requirements.txt
#
# For GPU support (CUDA 11.7+):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu117
#   Then install the rest: pip install -r requirements.txt

# ─── Core Deep Learning ───────────────────────────────────────────────────────
# PyTorch (install separately for GPU support - see above)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# PyTorch Geometric and dependencies
torch-geometric>=2.3.0
torch-scatter>=2.1.0
torch-sparse>=0.6.17
torch-cluster>=1.6.1
torch-spline-conv>=1.2.2

# ─── Data Processing ──────────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# ─── Network Traffic Analysis ─────────────────────────────────────────────────
scapy>=2.5.0

# ─── Web Framework & Dashboard ────────────────────────────────────────────────
Flask>=2.3.0
Flask-SocketIO>=5.3.0
python-socketio>=5.9.0
Werkzeug>=2.3.0

# ─── Windows Notifications ────────────────────────────────────────────────────
# Choose ONE of the following (winotify is recommended):
winotify>=1.1.0        # Recommended - modern Windows notifications
# win10toast>=0.9      # Alternative - older but stable

# ─── Utilities ────────────────────────────────────────────────────────────────
python-dateutil>=2.8.2
